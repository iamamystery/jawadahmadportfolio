// The hero lives in components/hero/ — it is composed of several parts
// (backdrop, instrument frame, globe canvas, CTAs). This re-export keeps
// `@/components/Hero` stable for app/page.tsx.
export { default } from "./hero/Hero";
